[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "fastorganize"
version = "0.1.0"
description = "Simple project scanner and analyzer utilities."
readme = "README.md"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["fastorganize"]
